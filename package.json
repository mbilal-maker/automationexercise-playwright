{
  "name": "automationexercise",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test1": "cucumber-js --require features/**/*.js",
    "contact1": "cucumber-js --tags @ContactUs",
    "addproduct1": "cucumber-js --tags @AddProduct",
    "productqty1":"cucumber-js --tags @ProductQty",
    "registerCO":"cucumber-js --tags @RegisterCheckout",
    "productqty":"cucumber-js --tags @ProductQty --require features/**/*.js --format progress --format json:reports/cucumber-report.json --parallel 2",
    "addproduct": "cucumber-js --tags @AddProduct --require features/**/*.js --format progress --format json:reports/cucumber-report.json --parallel 2",
    "test": "cucumber-js --require features/stepDefination/*.js --format progress --format json:reports/cucumber-report.json --parallel 2",
    "contact": "cucumber-js --tags @ContactUs --require features/**/*.js --format progress --format json:reports/cucumber-report.json --parallel 2",
    "report": "node reports/report.js",
    "test:report": "npm run test && npm run report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cucumber/cucumber": "^12.7.0",
    "@cucumber/html-formatter": "^20.0.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.0",
    "cucumber-html-reporter": "^6.0.0",
    "multiple-cucumber-html-reporter": "^3.10.0",
    "playwright": "^1.58.2"
  }
}
